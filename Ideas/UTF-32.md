A way of encoding [[Unicode|Unicode]] in [[Binary|binary]]. **UTF-32** (Unicode Transformation Format) is similar to [[UTF-16]] in design. It's still not [[ASCII]] compatible, and it's wasting even more space. However, every [[Unicode]] **code point** can be represented.

The only advantage it has over [[UTF-8]] is that it's simpler.