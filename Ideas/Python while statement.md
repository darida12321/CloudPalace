Repeatedly evaluate an exception and if it's `True`, run the first code block. If it's `False`, execute the `else` block. It can have [[Python break statement|break]] or [[Python continue statement|continue]] statements.
```python
while True:
	pass
else: 
	pass
```

The `else` block doesn't activate after `break`, which can be used to simplify code in some cases.