Nested inside a [[Python callable|function]]. In a [[Python callable|generator function]], it raises a `StopIteration` [[Python exception|exception]].
```python
def f(arg): return 1+2, ('a', True)
```