You can create a **footnote**[^name] using `[^name]`. Then define it later by `[^name]: `.

[^name]: Footnotes can contain multiple lines by adding 4 spaces or a tab.
    They can also use `code blocks` and other **styles**.