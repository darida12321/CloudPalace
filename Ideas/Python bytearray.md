Similar to [[Python bytes|bytes]], but it's mutable so it inherits the [[Python mutable sequences|mutable sequence]] operations too.

**Functions**:
- `.resize(n)`: Resize. Shrinking drops from end, growing adds null bytes.
