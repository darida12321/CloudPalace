Contains all of Python's **built-in** identifiers ([[Python built-in constants|constants]], [[Python built-in exceptions|exceptions]], [[Python built-in functions|functions]], [[Python built-in types|types]]).

So technically, the full name for `open()` is `builtins.open()`.
This usually isn't used, but can come in handy if you want to overwrite some of these.



