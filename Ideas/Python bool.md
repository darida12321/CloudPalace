Inherits from **[[Python int|int]]**. Represents an **boolean** value. Only has 2 instances: `True` and `False`.

**Constructor**:
- `True`
- `False`
- `bool(x)`: Objects are `True`, unless their `__bool__()` is `False`, or `__len__()` is `0`.

**Operations**:
- `not x`, `x and y`, `x or y`.
