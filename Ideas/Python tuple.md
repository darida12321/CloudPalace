Inherits from **[[Python immutable sequence|immutable sequence]]**. Represents a tuple of items, with different types..

**Constructor**:
- `()`, `(a,)`, `(a, b, c)`
- `tuple(iterable)`
**Functions**:
- `sort(key=None, reverse=False)`: Sorts the list in place, apply key function.


