Inherits from **[[Python immutable sequence|immutable sequence]]**. Produces a range of values. Useful because it only takes up one element of space in memory.

**Constructor**:
- `range(stop)`: Produce numbers until stop.
- `range(start, stop, step=1)`: Produce numbers from start to stop.

